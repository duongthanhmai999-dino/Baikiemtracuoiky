PORT=4000
NODE_ENV=development

# Allowed CORS origins (comma-separated). Use * for demo.
CORS_ORIGIN=*

# File JSON lưu tài khoản (đăng ký/đăng nhập). Đường dẫn relative tới thư mục chạy `be`.
USERS_JSON_FILE=db/users.json
